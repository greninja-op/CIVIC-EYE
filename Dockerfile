# This file is replaced by service-specific Dockerfiles
# See:
# - client/Dockerfile (React + Tailwind)
# - backend/Dockerfile (Laravel PHP)
# - realtime/Dockerfile (Node.js)
# - ai-service/Dockerfile (Flask Python)
